apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-demo
  namespace: flask-demo
  labels:
    application: flask-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      application: flask-demo
  template:
    metadata:
      labels:
        application: flask-demo
    spec:
      initContainers:
      containers:
        - name: flask-demo
          env:
          image: docker.io/neerajtiwari/flask-app:e370a131e667226ac48201ad9e5688e72a7747e9
          ports:
            - containerPort: 8001
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              cpu: 10m
              memory: 128Mi
